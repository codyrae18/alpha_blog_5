<h1> Listing all Categories </h1>
<% @categories.each do |category| %>
    <ul> 
        <div>
            <div> 
            <li>
                <%= link_to "#{category.name}", category_path(category) %>
            </li>
            </div>
        </div>
    </ul>
<% end %>    
    <%= will_paginate %> 